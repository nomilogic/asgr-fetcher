# Required server-side
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=
SUPABASE_BUCKET_NAME=

# Optional: direct DB SQL access (for migrations only; do not use in code)
DIRECT_URL=

# Optional: if you also build a client later
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
