{
  "name": "asgr-ingest-supabase",
  "version": "1.0.0",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "server:dev": "npm run dev --prefix server",
    "server:build": "npm run build --prefix server",
    "server:start": "npm run start --prefix server",

    "fetcher:dev": "npm run dev --prefix fetcher",
    "fetcher:build": "npm run build --prefix fetcher",
    "ingest:all": "npm run ingest:all --prefix fetcher",
    "truncate:all": "npm run truncate:all --prefix fetcher",
    "hs:fetch": "npm run hs:fetch --prefix fetcher",
    "hs:ingest": "npm run hs:ingest --prefix fetcher",
    "circuit:ingest": "npm run circuit:ingest --prefix fetcher",

    "admin:dev": "npm run dev --prefix admin",
    "admin:build": "npm run build --prefix admin",
    "admin:preview": "npm run preview --prefix admin",

    "client:dev": "npm run dev --prefix client",
    "client:build": "npm run build --prefix client",
    "client:preview": "npm run preview --prefix client"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {},
  "devDependencies": {}
}
